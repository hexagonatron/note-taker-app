{
  "name": "note-taker-app",
  "version": "1.0.0",
  "description": "An app that takes notes and saves them to a json file for retrival later",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.3"
  }
}
